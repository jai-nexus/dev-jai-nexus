- Required checks cannot be path-filtered

- portal_ci_guardrails must run on every pull_request

- Required status check context must match exactly:
  portal_ci_guardrails / portal_guardrails (pull_request)

- If you rename workflow/job, you must update the ruleset
