meta:
  quarter: "2026-Q1"
  version: "v0.1"
  locked: true
  locked_at: "2026-01-04"
  sources:
    repos: "../../repos.yaml"
    # domains can come next when you add portal/config/domains.yaml
  rules:
    - "repos.yaml is the canonical repo SoT"
    - "all wave planning derives from repos.yaml wave field"
    - "changes require version bump and lock-policy note"

q1:
  waves: [0, 1, 2, 3]
  wave_repo_selector:
    field: "wave"
    values: [0, 1, 2, 3]

normalization:
  # keep this even though you already fixed orchestrator
  repo_slug_fixes:
    - from: "jai-nexus/orchestrator nexus"
      to: "jai-nexus/orchestrator-nexus"
