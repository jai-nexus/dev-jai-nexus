## Execution

1. Import the static draft review model into `/operator/corpus`.
2. Render one compact section near the existing sandbox review and validator posture blocks.
3. Show source fixture and canon paths explicitly.
4. Validate motion package, agency posture, sandbox fixtures, snapshot, typecheck, and build.
