title: "Panels Operationalization v0: score UX + shared scorer + coverage signal"

target:
  domain: dev.jai.nexus
  repo: dev-jai-nexus

auto_ratify:
  enabled: false

max_risk_score: 0.2

checks_required:
  - validate_agency
  - typecheck

checks_optional:
  - dct_replay_check
