title: "Q1M3: Polyrepo Coverage v0 + Conductor Dispatch (dev-jai-nexus)"

target:
  domain: dev.jai.nexus
  repo: dev-jai-nexus

auto_ratify:
  enabled: true

# Keep this conservative; raise later once the loop feels stable.
max_risk_score: 0.20

checks_required:
  - validate_agency
  - dct_replay_check
  - typecheck
