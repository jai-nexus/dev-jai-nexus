# Proposal: Paid Beta Matrix Customer-Safe JAI Boundary Intake v0

## Purpose

Update `dev-jai-nexus` paid-beta readiness canon to include the settled `jai`
customer-safe substrate boundary from PR `#15`.

## Scope

- update the paid-beta readiness matrix with `jai` PR `#15` as evidence
- update next-routing posture to reflect that the substrate boundary is now settled
- add a short intake artifact clarifying what `jai` owns and does not own
- refresh the bundled motion snapshot through `motion-0237`

## Non-goals

- mutating `jai`
- mutating `jai-nexus`
- mutating `api-nexus`
- mutating `audit-nexus`
- mutating `docs-nexus`
- authorizing implementation
- opening paid beta
