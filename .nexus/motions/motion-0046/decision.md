# Decision - motion-0046

## Status
RATIFIED — unanimous consent.

## Summary
The corrected Phase 1 model staffing manifest has been ratified and committed
as `.nexus/model-slots-phase1.yaml`. The manifest governs 10 live executor
slots across 5 panels (Anthropic + OpenAI), 5 deferred selector slots, and
3 voting role assignments (Proposer, Challenger, Arbiter).

## Outcome
All three required roles voted YES. No objections, no reservations.

## Follow-up noted
- Reconcile legacy `model-slots.yaml` and `model-routing.yaml` with the new Phase 1 file in a future motion.
- Activate selector slots when the evaluation harness is ready.
