title: "Dispatch v0: config scaffold + seed for dev-jai-nexus (CRL-1 → CRL-2)"

target:
  domain: dev.jai.nexus
  repo: dev-jai-nexus

auto_ratify:
  enabled: true

max_risk_score: 0.15

checks_required:
  - validate_agency
checks_optional: []
