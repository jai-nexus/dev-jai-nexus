title: "Panel Selection Runner v0: compute totals + winner from selection record"

target:
  domain: dev.jai.nexus
  repo: dev-jai-nexus

auto_ratify:
  enabled: false

max_risk_score: 0.15

checks_required:
  - validate_agency
  - typecheck

checks_optional:
  - dct_replay_check
