Illustrative example only. This file is not a live governance artifact.

# Orchestrator Example

This example shows the recurring orchestrator artifact chain in a bounded,
fictional seam. It is advisory only and does not imply automation, runtime
control, automatic routing, branch creation, or motion creation.

---

## Candidate seam comparison

```markdown
# Candidate Seam Review

## Current repo framing

- repo: `example-repo`
- active program: `example-program`
- settled canon relevant to this review:
  - `motion-WWWW`
  - `example-governance-model.md`
- current boundary: documentation-only seam selection; no runtime or command-surface changes

## Candidate seams

### Seam A - example-orchestrator-canon

- description: define the missing orchestrator recommendation layer
- why viable now: adjacent control-thread and repo-execution docs already exist
- what it unblocks: reusable recommendation and repo-init packaging
- risk if deferred: future sessions keep reconstructing routing structure from memory

### Seam B - example-exploration-canon

- description: define the open-ended framing layer
- why viable now: adjacent coordination docs exist
- what it unblocks: clearer ambiguity reduction before execution
- risk if deferred: exploration remains under-specified

## Recommendation

- recommended next seam: `example-orchestrator-canon`
- receiving coordination mode: `REPO_EXECUTION`

## Why this seam is next

- bounded rationale: it closes the layer directly between settled continuity and fresh repo execution
- timing rationale: it depends only on already settled adjacent canon
- fit rationale: the next receiving session needs bounded documentation work, not further exploration

## Seams explicitly deferred

- deferred seam: `example-exploration-canon` - reason: useful, but not the next blocking gap
```

---

## Next-motion recommendation

```markdown
# Next Motion Recommendation

## Recommended motion

- recommended motion title: `Example Orchestrator Canon v0`
- subtitle: `advisory routing and next-seam packaging`
- recommended motion id: `motion-XXXX`
- kind: `builder-proof`
- program: `example-program`
- basis: `motion-WWWW`

## Exact scope

- bounded seam statement: define the orchestrator model plus the three recurring routing templates
- included deliverables:
  - `.nexus/docs/example-orchestrator-model.md`
  - `.nexus/docs/templates/example-candidate-seam-template.md`
  - `.nexus/docs/templates/example-next-motion-recommendation-template.md`
  - `.nexus/docs/templates/example-repo-init-routing-template.md`

## Non-goals

- runtime routing
- portal mutation
- automatic branch or PR generation

## Risks

- Risk: orchestrator language could drift into execution authority
  Mitigation: restate recommendation-only boundaries in every artifact

## Likely touched paths

- new files:
  - `.nexus/docs/example-orchestrator-model.md` - define the advisory routing layer
  - `.nexus/docs/templates/example-candidate-seam-template.md` - reusable comparison shape
- modified files:
  - none
- explicitly excluded paths:
  - `portal/**`
  - `.claude/**`

## Recommended branch

- recommended branch: `sprint/example-orchestrator-canon`
```

---

## Repo-init routing package

```markdown
# Repo Init Routing Package

## Repo baseline

- repo name: `example-repo`
- domain: `example.domain`
- current branch: `sprint/example-orchestrator-canon`
- last commit: `abcd123 example baseline commit`
- working tree state: `clean`

## Settled canon

- relevant ratified motions:
  - `motion-WWWW`
- active template or schema versions:
  - `passalong-1.1`
- active canon docs:
  - `.nexus/docs/example-control-thread-model.md`
  - `.nexus/docs/example-repo-execution-model.md`

## Active seam

- intended motion: `motion-XXXX`
- bounded seam description: define the advisory orchestrator layer and its reusable routing templates
- receiving coordination mode: `REPO_EXECUTION`
- receiving execution role: `BUILDER`

## Request

- implement the bounded documentation artifacts for the orchestrator seam and keep the work strictly advisory

## Return shape

- expected outputs / artifacts:
  - orchestrator model doc
  - three routing templates
  - one illustrative example
- expected governance state change:
  - none at init stage

## Keep-out-of-scope constraints

- paths that must not change:
  - `portal/**`
  - `.claude/**`
- governance artifacts that must not be mutated:
  - `decision.yaml`
  - `vote.json`
- actions not authorized:
  - automatic branch creation
  - PR generation
```

---

## Recurring chain summary

The recurring orchestrator chain is:

1. candidate seam comparison
2. next-motion recommendation
3. repo-init routing package

That chain recommends and packages the next bounded seam. It does not execute the
seam for the receiving thread.
