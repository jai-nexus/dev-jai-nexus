# /operator/agents dashboard-state-index/v0 Sketch

## 1. Status

Scope: `/operator/agents dashboard-state-index/v0 Sketch`.

Mode: `REPO_EXECUTION / DOCS-REFERENCE / DASHBOARD-STATE-SKETCH / OPERATOR-SURFACE / AGENT-REGISTRY / STATIC-DATA / NO-RUNTIME / NO-EXECUTION`.

Branch: `docs/q2-operator-agents-dashboard-state-index-v0-sketch`.

Artifact status: docs/reference dashboard-state-index sketch.

This sketch is reference-only. It is not parser-ready unless a later
CONTROL_THREAD route explicitly accepts a parser-ready object.

It grants no UI, route, navigation, dashboard-state, route-state,
project-state, repo-lane, receipt, backend, runtime, Agent, Palette, vote,
model/provider, GitHub/tool, or gate authority.

ZERO GATES GRANTED.

## 2. Current Accepted Baseline

Accepted alignment baseline:

- `dashboard-state-index/v0 + route/v0 Alignment Review`.
- Accepted artifact: `docs/reference/dashboard-state-route-alignment-review-v0.md`.
- Accepted route: `/operator/agents`.
- Accepted topology: Option C - keep `/operator/agents` canonical and use
  local-only in-page navigation.

Relevant accepted surface baseline:

- `Agent Registry Read-Only Surface v0`.
- `Agent Registry Read-Only Surface v0 QA + Operator Slate Density Pass`.
- `Agent Vote Review Surface v0`.
- `Palette Agent Recommendation Surface v0`.
- `Agent Registry Local Surface Navigation v0`.

## 3. Sketch Purpose

This sketch records how `/operator/agents` could be safely represented by a
future `dashboard-state-index/v0` object.

The sketch is not a dashboard-state object. The sketch is not dashboard
activation. The sketch is not route creation. The sketch is not route-state
mutation. The sketch is not live verification. The sketch is not gate opening.

dashboard-state-index/v0 represents dashboard state index structure.

dashboard-state-index/v0 does not create dashboard state.

dashboard-state-index/v0 does not activate dashboards.

dashboard-state-index/v0 does not create project state.

dashboard-state-index/v0 does not create repo lanes.

dashboard-state-index/v0 does not create routes.

dashboard-state-index/v0 does not mutate route state.

dashboard-state-index/v0 does not create Agents.

dashboard-state-index/v0 does not activate Agents.

dashboard-state-index/v0 does not dispatch Agents.

dashboard-state-index/v0 does not open gates.

## 4. Dashboard Surface Identity

Sketch fields:

- `dashboard_surface_id`: `operator-agents-agent-registry`
- `surface_family`: `operator_agent_registry`
- `surface_category`: `read_only_static_registry_surface`
- `owner_route`: `/operator/agents`
- `accepted_topology_reference`:
  `docs/reference/operator-agent-registry-route-topology-surface-split-decision-v0.md`

Stable display name: Operator Agent Registry.

This identity is a future index reference only. It does not create dashboard
state, activate dashboards, create project state, create repo lanes, create
routes, create Agents, activate Agents, dispatch Agents, open gates, create
receipts, or update canon.

## 5. Route Reference

Sketch fields:

- `route_path`: `/operator/agents`
- `canonical_route_posture`: canonical Agent Registry route
- `route_compatibility`: preserved
- `local_anchor_route_status`: anchors are not routes
- `adjacent_subroutes_authorized`: false

`/operator/agents` remains canonical. Local anchors are not routes. No adjacent
subroutes are authorized by this sketch. Any future route split requires
separate CONTROL_THREAD acceptance.

Navigation does not authorize action.

Route promotion does not open gates.

## 6. Surface Title And Status

Sketch fields:

- `surface_title`: `Operator Agent Registry`
- `surface_status`: `accepted_read_only_static_surface`

The accepted route remains canonical. The status describes display/index
posture only. It is not dashboard activation, route promotion, Agent
activation, recommendation acceptance, vote decision, receipt creation, canon
update, or gate opening.

## 7. State Kind And Data Posture

Sketch fields:

- `state_kind`: `static_read_only_surface_index`
- `data_posture`: `static_data_not_live_verification`

Static display is not live state. Static display is not runtime verification.
Static display is not operational truth without review. Dashboard display does
not authorize.

The future index should treat `/operator/agents` as a static read-only surface
reference with source artifacts and staleness notes, not as live dashboard
state or live operational state.

## 8. Static Data Sources

Sketch field:

- `static_data_sources`

Accepted static data source references:

- `portal/src/data/operator/agentRegistry/`
- `portal/src/data/operator/agentRegistry/roleTemplates.ts`
- `portal/src/data/operator/agentRegistry/domainEngines.ts`
- `portal/src/data/operator/agentRegistry/repoDomainScope.ts`
- `portal/src/data/operator/agentRegistry/projectAgentCandidates.ts`
- `portal/src/data/operator/agentRegistry/paletteRecommendations.ts`
- `portal/src/data/operator/agentRegistry/agentVotes.ts`
- `portal/src/data/operator/agentRegistry/index.ts`

These are static source references only. There is no live fetch, runtime
verification, persistence, mutation, hidden storage, API call, server action,
DB/Prisma read/write, provider/model call, GitHub/tool integration, or Agent
dispatch.

## 9. Visible Labels

Sketch field:

- `visible_labels`

Required visible labels:

- `READ-ONLY`
- `STATIC DATA`
- `NON-EXECUTING`
- `ADVISORY ONLY`
- `RECOMMENDATION ONLY`
- `PALETTE RECOMMENDATION  NOT CREATION`
- `NOT ACTIVATION`
- `NOT DISPATCH`
- `NOT AUTHORITY`
- `CONTROL_THREAD REVIEW REQUIRED`
- `ZERO GATES GRANTED`
- `LOCAL NAVIGATION`

These labels make the surface safe for future dashboard-state indexing because
they distinguish static display from live state, Agent Registry display from
Agent activation, Palette recommendation from Agent creation, vote review from
decision authority, local navigation from route creation, and dashboard display
from authorization.

## 10. Visible Doctrine

Sketch field:

- `visible_doctrine`

Dashboard/route topology boundary:

- `Dashboard display does not authorize.`
- `Navigation does not authorize action.`
- `Route promotion does not open gates.`

Agent Registry non-activation boundary:

- `Agent Registry display does not activate Agents.`
- `Agent registry entries do not execute.`
- `Agent lanes do not execute.`

Palette recommendation boundary:

- `Palette display does not create Agents.`
- `Palette recommendation is not creation.`
- `Palette recommendation is not activation.`
- `Palette recommendation is not dispatch.`
- `Palette recommendation is not authority.`

Vote advisory boundary:

- `Vote display does not decide.`
- `Agent votes are evidence, not authority.`
- `Agent votes do not decide.`
- `Quorum is readiness evidence, not approval.`

Receipt/acceptance boundary:

- `receipt records.`
- `acceptance decides.`

CONTROL_THREAD authority boundary:

- `CONTROL_THREAD decides.`
- `ZERO GATES GRANTED.`

## 11. Local Navigation Mode

Sketch field:

- `local_navigation_mode`: `static_fragment_anchor_navigation`

Static fragment anchors preserve route compatibility. Anchors are not routes.
Anchors do not create route state. Anchors do not persist workflow state.
Anchors do not create dashboard state. Anchors do not authorize action.

Future dashboard-state indexing may record local anchor groups as surface
organization only. It should not promote them to route records or workflow
state.

## 12. Surface Groups

Sketch field:

- `surface_groups`

Current local surface groups:

- Overview.
- Registry.
- Domain Engines.
- Polyrepo Scope.
- Service Domains.
- Candidates.
- Votes.
- Palette.
- Risk / Frozen.
- Authority Boundary.

Groups are display organization only. Groups do not authorize action. Groups do
not create dashboard state, route state, project state, repo lanes, votes,
recommendations, receipts, canon updates, or gates.

## 13. Risk Posture

Sketch field:

- `risk_posture`

Current risk posture: low-to-medium due to static data staleness and dense
route surface.

Tracked risks:

- Display could be mistaken for live dashboard state.
- Palette display could be mistaken for Agent creation.
- Vote display could be mistaken for decision authority.
- Receipt references could be mistaken for receipt creation.
- Anchors could be mistaken for routes.
- Labels could be weakened later to reduce density.

Containment:

- keep visible labels and doctrine;
- carry staleness notes;
- keep local anchors as surface organization only;
- keep `authority_boundary` as `display_only_non_authorizing`;
- keep `gate_posture` as `closed`.

## 14. Authority Boundary

Sketch field:

- `authority_boundary`: `display_only_non_authorizing`
- `non_authorizations`

CONTROL_THREAD decides.

Dashboard display does not authorize.

Agent Registry display does not activate Agents.

Palette display does not create Agents.

Vote display does not decide.

receipt records.

acceptance decides.

ZERO GATES GRANTED.

## 15. Gate Posture

Sketch field:

- `gate_posture`: `closed`

ZERO GATES GRANTED.

No execution gates opened. No Agent gates opened. No route gates opened. No
dashboard activation gates opened. No recommendation gates opened. No vote
progression gates opened. No receipt/canon gates opened.

## 16. Source Artifacts

Sketch field:

- `source_artifacts`

Source artifacts:

- `source`: `docs/reference/dashboard-state-route-alignment-review-v0.md`
- `unresolved_reference`: `docs/specs/profiles/dashboard-state-index-v0.md`
- `unresolved_reference`: `docs/specs/profiles/route-v0.md`
- `unresolved_reference`: `docs/specs/profiles/project-state-v0.md`
- `unresolved_reference`: `docs/specs/profiles/receipt-v0.md`
- `unresolved_reference`: `docs/specs/profiles/agent-role-template-v0.md`
- `unresolved_reference`: `docs/specs/profiles/agent-registry-entry-v0.md`
- `unresolved_reference`: `docs/specs/profiles/agent-domain-engine-v0.md`
- `unresolved_reference`: `docs/specs/profiles/agent-set-recommendation-v0.md`
- `source`:
  `docs/reference/operator-agent-registry-route-topology-surface-split-decision-v0.md`
- `source`: `docs/reference/polyrepo-agent-registry-static-data-v0.md`

The `docs/specs/profiles/*` paths above were not present in the local repo at
the time of this sketch. They are recorded as unresolved references because the
accepted route names them as profile drafts. This sketch does not infer content
from missing files.

## 17. Open Gaps

Sketch field:

- `open_gaps`

Open gaps:

- no formal parser-ready dashboard-state-index object.
- no live verification.
- no staleness policy accepted.
- no route/v0 sketch yet.
- no profile-backed vote bundle fields.
- no profile-backed Palette source-template/domain-engine fields.
- no adjacent subroute decision accepted.
- no local navigation accessibility QA receipt accepted.

## 18. Staleness Notes

Sketch field:

- `staleness_notes`

Static registry display may become stale. A future dashboard-state index should
carry a review timestamp or receipt reference after those concepts are
accepted. Static data should not be treated as live verification. A future
dashboard-state-index object should include source artifact references and
staleness posture.

No live runtime verification is performed by this sketch.

## 19. Non-Authorizations

This artifact does not authorize:

- execution
- runtime activation
- provider/model dispatch
- live model calls
- Agent execution
- Agent dispatch
- Agent creation
- Agent activation
- Agent instantiation
- tool invocation
- model calls
- GitHub integration
- GitHub API use
- route changes
- navigation changes
- redirects
- UI changes
- repo mutation outside docs/reference
- file mutation outside docs/reference
- branch/PR automation
- browser/desktop control
- terminal/command execution
- scheduler
- autonomous loop
- retrieval engine
- automatic context injection
- live memory writes
- hidden persistence
- live settings mutation
- new API routes
- new server actions
- DB writes
- Prisma changes
- telemetry
- auth/session changes
- customer-data handling
- production behavior
- `.jai` parser/runtime behavior
- `.jai` execution behavior
- `.nexus` active semantics
- policy enforcement
- execution gates opened
- automatic scoring
- automatic synthesis
- automatic best-agent selection
- automatic gate evaluation
- automatic profile validation
- recommendation submission
- recommendation persistence
- dashboard state creation
- dashboard activation
- project-state creation
- repo-lane creation
- route creation
- route-state mutation
- vote creation
- vote submission
- receipt creation
- canon update
- motion-state mutation
- gate opening

## 20. Recommended Next Route

Recommended next route: `/operator/agents route/v0 Sketch`.

Recommended mode:
`REPO_EXECUTION / DOCS-REFERENCE / ROUTE-SKETCH / OPERATOR-SURFACE / AGENT-REGISTRY / NO-RUNTIME / NO-EXECUTION`.

Recommended scope:

- Sketch `/operator/agents` as a route/v0-compatible recommendation.
- Preserve `/operator/agents` as canonical.
- Preserve local anchors as non-route surface organization.
- Reference this dashboard-state-index sketch as context.
- Do not create routes, mutate route state, alter navigation, add redirects,
  change UI, create dashboard state, create project state, create repo lanes,
  create receipts, update canon, open gates, or authorize Agent execution or
  dispatch.

Alternative next route: `Agent Registry Local Surface Navigation Accessibility QA v0`.

Optional future route: `Static Agent Registry Staleness Policy v0`.
